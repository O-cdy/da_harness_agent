# E-NNNN · <一句话标题>

> 复制本模板新建错误档案，编号递增；归档后到 `index.md` 登记一行。

| 字段 | 内容 |
|---|---|
| 编号 | E-NNNN |
| 发现日期 | YYYY-MM-DD |
| 模块 | orchestrator / ingest / executor / validator / evaluator / reporter |
| 状态 | open / fixed / archived |
| 关联 ADR | ADR-XXX |
| 关联规则 | R-XX |

## 1. 现象

（粘贴**原始报错原文**，不要转述。日志行 / 退出码 / 输出片段。）

## 2. 触发条件

（什么输入、什么数据、什么配置下必现。可复现的最小条件。）

## 3. 根因

- 表象根因：
- 架构根因：（若为架构级问题，必须写明，并回写 `docs/` 相应文件）

## 4. 最小复现

```
（命令 / 输入 / 数据片段）
```

## 5. 修复

（改了什么，文件与行。禁止只修现象不修根因。）

## 6. 防回归门禁

（新增的断言 / 测试 / 规则编号。没有门禁 = 未闭环。）

## 7. 复盘备注

（是否同类复发第 N 次；是否需升级为硬规则写入 `docs/30-constraints/rules.md`。）
