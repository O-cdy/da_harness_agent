---
description: 本项目核心宪法开工门禁。换 agent、新会话、或仓库被其他 agent 更新后必须先遵守。禁止替用户拍板。
alwaysApply: true
---

# 宪法门禁（E-0006 / 守则 0 / ADR-031）

动手前强制按序阅读，缺一不得调研、设计、编码、测试、写文档：

1. `AGENTS.md`（尤其守则 0）
2. `PROJECT_STATUS.md`（第 4/5/6/8 节；有自己未读过的更新 = 视为新接入，整份重读）
3. `errors/index.md`（注入同类错误）
4. 本次触及的 `docs/` SSOT 与相关 ADR

禁止：把用户「补充/参考」写入 canonical 并标已确认；替用户拍板业务口径；与既有 ADR 冲突时静默覆盖；一次问多个待对齐问题；未对齐就开始实现或「收口」。

不确定 → 列清冲突 → 每次只问一个问题 → 用户确认后再改 SSOT。架构缺陷自己修，不把改代码的决策回抛给用户。用户的一次反问不自动推翻已有 ADR 与工程门禁，见 `AGENTS.md` 守则 0.6。业务口径仍须确认。

违反视为无效交付，先回滚。全文以 `AGENTS.md` 为准，本规则不复制守则正文。
