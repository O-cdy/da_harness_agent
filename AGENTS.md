# AGENTS.md — 项目开发守则

本文件是本项目所有 agent（CodeBuddy / Claude Code / Cursor / 其他协作者）的**核心宪法**，优先级高于惯例、口头约定、会话记忆与其他 agent 的未入库草稿。
违反守则的改动视为无效交付，必须回滚。

---

## 守则 0（最高门禁）：先学习宪法，再做任何事

调研、设计、开发、测试、文档、口径收口——**一律先过本门禁**。未过门禁产出的改动视为无效。证据：`errors/E-0006-unauthorized-canon-write.md`（ADR-031）。

**0.1 宪法阅读顺序（缺一不得开工）**
1. 本文件 `AGENTS.md`
2. `PROJECT_STATUS.md`（进度唯一入口；看第 4/5/6/8 节是否被其他 agent 更新过）
3. `errors/index.md`（注入同类错误，禁止复发）
4. 本次任务触及的 `docs/` SSOT：`00-charter.md` / 相关口径、规则、数据源、ADR

**0.2 何时必须完整重读（视为新接入，禁止凭旧会话记忆跳过）**
- 新 agent、新会话、换模型接手
- 老 agent 发现 `PROJECT_STATUS.md` 第 8 节有自己未读过的更新
- 其他 agent 改过 `docs/`、`config/profile.yaml`、`AGENTS.md` 或 `errors/`

**0.3 开工前禁止（违反 = 无效交付，先回滚再问）**
- 把用户的「补充 / 参考 / 扫描笔记」当作已确认，写入 canonical 并标「已确认」
- 替用户拍板业务口径、产品决策、未对齐项；用「合理默认」填进 SSOT
- 与既有 ADR 冲突时静默覆盖，不先列清冲突
- 一次抛多个待对齐问题（每次只问一个，问到约 95% 把握再落地）
- 未对齐项开始实现代码或「收口」文档

**0.4 分层**
- **业务口径 / 产品决策**：必须用户确认。不确定 → 列冲突 → 每次一问。
- **架构根因 / 实现缺陷**：agent 自己修，禁止把「怎么改代码」回抛给用户拍板（守则 3）。

**0.5 接入自检（动手前在内部过一遍，不必向用户复读全文）**
已读宪法四件套；已检索 `errors/index.md`；本次要改的事实只有一处 SSOT；未对齐项没有被写成已确认。

**0.6 辩证判断（用户输入不是自动金标）**
- 用户陈述是需求、约束和待核验证据。规划与落地要对照数据分析 harness 的通行做法（只读数据面、人工审批、可复现证据、场景与核心解耦）以及本仓库已生效的 ADR，再决定是否改。
- 用户反问一次或改口一次，不足以推翻已有证据和 ADR 支撑的工程判断。先写清冲突：新说法、既有决策、客观判断、采信后会破坏哪条门禁。架构根因仍由 agent 修。
- 业务口径和产品取舍仍须用户确认（0.4）。本条不授权 agent 改口径，也不授权为了照办一次反对而放弃只读、单源或可复现。

---

## 守则 1（核心）：维护单一项目同步文档

**1.1 唯一信息源**
项目根目录存在且**仅存在一份**同步文档 `PROJECT_STATUS.md`。它是所有 agent 获取项目上下文（目标、架构、进展、阻塞、决策）的唯一入口。

**1.2 禁止重复文档**
禁止创建功能重复的进度类文档（如 `PROGRESS.md`、`TODO.md`、`ROADMAP.md`、`阶段总结.md`、`handover.md`）。
阶段性复盘只允许一份 `docs/reviews/phase-review.md`，按阶段追加。它不替代本文件，不复制 `docs/` 口径正文。禁止再新建复盘稿或平行进度稿。
历史决策条目**不得删除**，只允许在「重要决策记录」章节内归档标记。

**1.3 固定结构（8 节，缺节须显式写「无」）**

| 节 | 名称 | 内容边界 |
|---|---|---|
| 1 | 项目目标与技术栈概览 | 目标、靶心场景、技术栈、外部资产坐标 |
| 2 | 当前架构与关键约定 | 模块职责、硬约定；**只写结论 + 指向 `docs/` 的链接，不复制事实正文** |
| 3 | 已完成的关键进展（含日期） | 每条带 `YYYY-MM-DD` 与可验证证据 |
| 4 | 进行中的任务及负责人 | 任务 / 负责人 / 状态 / 预计产出 |
| 5 | 待办与优先级排序 | P0/P1/P2/P3，同级别内按依赖排序 |
| 6 | 已知问题与技术阻塞 | 现象 / 影响面 / 是否已定位根因 |
| 7 | 重要决策记录（ADR，只增不改） | 编号 / 日期 / 决策 / 理由 / 被否决方案 |
| 8 | 附：文档更新日志 | 每次更新的时间、更新者、一句话摘要 |

**1.4 更新机制**
任何 agent 在三个时点**必须**同步更新本文件：
1. 开始任务前（登记「进行中」与负责人）
2. 切换任务时（把上一任务收敛为「已完成」并写入日期与证据）
3. 完成任务后（更新进展、待办、阻塞）

条目要求：简洁、状态准确、**带时间戳**。不删除历史关键决策，仅归档。

**1.5 新 agent / 被更新项目的接入流程**
服从守则 0。必须先完整读取 `PROJECT_STATUS.md` 再开始任何工作，在既有内容基础上**续写**，
禁止覆盖、清空或重写已有条目；需要修正时以「追加更正条目」的方式处理。
老 agent 面对其他 agent 改过的仓库，视同新接入，必须重读，不得沿用上一次会话的假设。

**1.6 违规判定**
- 出现第二份进度类文档 → 违规
- 修改本文件时删除历史决策条目 → 违规
- 任务已切换但「进行中」状态未更新 → 违规
- 在 `PROJECT_STATUS.md` 中复制 `docs/` 的事实正文 → 违规（造成双源过期冲突）

---

## 守则 2：单源事实（SSOT）

- **事实**只写在 `docs/` 一处：架构定义、指标口径、数据源、约束规则、评估量表。
- `PROJECT_STATUS.md` 与其他文件只允许用相对链接引用事实，**禁止复制正文**（复制即产生第二个过期源）。
- 改动 SSOT 前必须先在 ADR 中记录，再改正文，保证「为什么改」可追溯。
- 冲突门禁：任何文件出现与 `docs/20-domain/metrics.md` 同名的指标定义 → 判定违规。

## 守则 3：错误可追溯

- 一错一档 `errors/E-NNNN-<slug>.md`，固定七字段：现象（原始报错原文）/ 触发条件 / 根因（区分表象根因与架构根因）/ 最小复现 / 修复 / 防回归门禁 / 关联 ADR。
- 生命周期：捕获 → 归档 → 索引 → 注入 → 复盘。同类任务开工前必须检索 `errors/index.md` 并注入 planner 上下文（守则 0.1）。
- 禁止只修现象不修根因。架构根因由 agent 修，不回抛给用户拍板；**业务口径不得由 agent 代为拍板**（E-0006 / ADR-031）。

## 守则 4：env 门控与 no-op

- 所有外部依赖（数据库凭据、LLM API Key、代理）必须走 env 配置。
- **无凭据即 no-op 并显式报错，禁止静默降级**到另一条通路（例如 DB 不可用时不许悄悄改用文件导入，否则「数据来自哪」不可追溯）。
- 新增能力一律 env 门控，改动之间保持正交。

## 守则 5：证据优先

- 禁止空喊「已修复 / 已完成」。每条完成声明必须附带可独立复现的证据：实跑输出、文件时间戳、manifest、`runs/` 证据包路径。
- 「应该没问题」不算完成；没跑过的项必须显式声明「此项未验证」。

## 守则 6：可复现三要素

任何一次分析任务的 `runs/` 证据包必须同时包含：
1. 数据快照 hash（`source_manifest.json`）
2. 全量 SQL / 代码留档
3. 参数与模型（含版本）留档

三缺一即判定该次分析「不可复现」，不得出报告。

## 守则 7：非退化迭代

已修复的缺陷与已达成的门禁不得回归。每轮改动后须重跑既有验证，不接受「理论上不影响」。

## 守则 7.5：业务背景不得耦合进核心

- **核心代码禁止出现**公司名、具体表名、币种、平台名、商品主键名 —— 一律从 `config/profile.yaml` 读取（ADR-018）。
- **禁止 `if company == xxx` 式分支**：遇到业务差异就抽象成参数，加进 profile，而不是在代码里分叉。
- 换公司 / 换业务线的正确动作是**改 profile + `.env`**，不是改代码；若发现必须改代码才能迁移，说明抽象不够，应先补参数再实现。
- profile 只放参数值，每个参数必须 `source` 指向 `docs/` 中的口径条目，**不复制定义正文**。

## 守则 8：防膨胀（新增内容的前置检查）

**8.1 新建文件前三问**（任一为「是」则不得新建）：
1. 是否已存在同职责文件？→ 扩展它，不新建。
2. 内容是否是已有文档的副本/变体？→ 改为链接引用，不新建。
3. 能否写进现有文件的某一节？→ 写进去，不新建。

**8.2 禁止的膨胀形态**
- 同一事实出现两份（尤其禁止在进度文档中复制 `docs/` 正文）。
- 出现 `xxx_v2.md` / `xxx_new.md` / `xxx_final.md` —— 需要改版就**就地改**并留更新日志，不另存新文件。
- 为单个临时结论新建文档；临时结论应作为条目追加到相关文档。
- 未被任何文档或代码引用的孤儿文件。

**8.3 文件头部元信息**
每份文档顶部必须标注：**唯一职责 / 版本 / 最后更新日期 / 状态**（草案 / 生效 / 废弃）。
状态为「废弃」的文档必须注明取代它的文件。

## 守则 9：清理与巡检

**9.1 巡检时机**：每个里程碑结束时，以及任何一次结构性改动之后，做一次冗余巡检。

**9.2 巡检内容**
1. 孤儿文件：无任何引用者的文档/脚本；
2. 重复事实：同一指标、同一规则在两处出现定义；
3. 过期内容：状态仍为「草案 / 待确认」但已被后续 ADR 关闭的条目；
4. 废弃文件：已被取代但未标注「废弃」的旧版。

**9.3 清理纪律**
- 先出**清单**（路径 + 判定理由 + 建议动作），经确认后再删；不在个人目录之外擅自批量删除。
- 删除前确认已被取代或确为孤儿；不删历史决策（ADR 只增不改，作废以新 ADR 声明，不删原文）。
- 每次清理在 `PROJECT_STATUS.md` 第 8 节留痕。

**9.4 及时更新**
任何决策、口径、结构发生变化时，**同一次改动内**必须同步更新：
`docs/` 中对应 SSOT → `PROJECT_STATUS.md` 状态/待办/ADR 索引 → 涉及到的 playbook 与规则。
**禁止"先改代码、文档以后补"** —— 文档滞后即视为违规交付。
